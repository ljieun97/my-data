import TimetablePlanner from "@/components/contents/timetable-planner";

export const metadata = {
  title: "Timetable",
};

export default function TimetablePage() {
  return <TimetablePlanner />;
}
